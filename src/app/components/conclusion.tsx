import Section from "./section"

export default function Conclusion() {
    return (
        <Section title="Conclusion">
            <p>
                In this project, we attempt to generate differentially private synthetic data for an Intel telemetry dataset.
            </p>
        </Section>
    )
}